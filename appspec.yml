version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/laravel
  - source: infrastructure//laravel.conf
    destination: /etc/nginx/sites-available/laravel
permissions:
  - object: /var/www/laravel
    pattern: "**"
    owner: ec2-user
    group: adm
hooks:
  BeforeInstall:
    - location: infrastructure/scripts/install_dependencies.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: infrastructure/scripts/set_permissions.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: infrastructure/scripts/post_install.sh
      timeout: 300
      runas: root
